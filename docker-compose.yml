services:

  app:

    build:
      context: .
      dockerfile: Dockerfile

    container_name: linkedin-profile-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped

    mem_limit: 512m

    cpus: 0.1
