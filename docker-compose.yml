services:

  app:

    build:
      context: .

    container_name: linkedin-profile-api

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped

    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.10"
